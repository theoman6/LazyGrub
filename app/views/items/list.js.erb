var info = '<%= j(items_collection(@order, @rest)) %>'
$('#items-list').html($("<div/>").html(info).text());
$(".item_quantities").change(function() {
		get_prices();
	});
get_prices();
$(".accordion").accordion({
	heightStyle: 'content',
	collapsible: true,
	autoHeight: false,
	active :false
});
